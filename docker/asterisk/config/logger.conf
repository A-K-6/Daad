[general]
[logfiles]
console => notice,warning,error
messages => notice,warning,error
; Sanitized captures only: full SIP debug is OFF by default so secrets
; never land in committed logs. Enable locally with
;   asterisk -rx "pjsip set logger on"
; and redact before sharing (see scripts/redact-pcap.sh).
