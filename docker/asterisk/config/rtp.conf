; RTP range per JBM contract: 10000–10199 (200 ports = ~100 calls headroom
; for the fixture; prod sizing is out of scope for this test rig).
[general]
rtpstart = 10000
rtpend = 10199
; Strict RTP learning supports symmetric-RTP + VPN/sleep recovery.
strictrtp = yes
icesupport = no
stunaddr =
